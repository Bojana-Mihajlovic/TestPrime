import math 
from delilac import deli 

def is_prime(x):
        delioci = deli(x)
        if x==1 or (x>1 and len(delioci)>2):
                return False 
        else: 
                return True 
 